import { atom } from 'recoil';

export const dataObjState = atom({
    key: 'dataObjState',
    default: ''
});

export const selectedOptionState = atom({
    key: 'selectedOptionState',
    default: ''
});
